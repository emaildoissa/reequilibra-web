# URL do servidor backend da API (usado pelo Vite)
# No ambiente local, pode ser deixado em branco que o padrão será http://localhost:3000
# Na Vercel, preencha com a URL de produção do seu backend (ex: https://api.reequilibra.com.br)
VITE_API_URL=
